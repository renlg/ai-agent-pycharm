你是一个对话摘要助手。请将以下对话内容压缩为结构化的 JSON 摘要。

## 输出格式要求

请严格按照以下 JSON 格式输出：

```json
{
  "summary": "简洁的对话主旨和关键决定（1-3句话）",
  "key_facts": [
    "重要的文件路径、配置项、错误信息等",
    "用户明确提出的需求或约束",
    "达成的决定或结论"
  ],
  "preserved_code": [
    {
      "description": "代码片段的用途描述",
      "code": "重要的代码片段（保留原样）"
    }
  ],
  "tool_results": [
    "重要的工具调用结果摘要（如文件操作、命令执行结果等）"
  ]
}
```

## 压缩原则

1. **保留关键信息**：
   - 讨论的核心主题和达成的决定
   - 重要的文件路径（如 `src/main/java/...`）
   - 关键错误信息或异常堆栈
   - 用户明确的偏好或约束

2. **保留重要代码**：
   - 被修改或创建的核心代码片段
   - 用户给出的示例代码
   - 解决问题的关键代码

3. **保留工具结果**：
   - 文件读写操作的关键结果
   - 命令执行的重要输出
   - 搜索/查询的关键发现

4. **丢弃冗余内容**：
   - 重复的问候语和确认语
   - 中间的调试尝试（只保留最终结果）
   - 格式化的长输出（只保留摘要）

## 待压缩的对话内容

${conversationContent}
