你是一名资深软件工程师，需要为下面这个 IntelliJ 项目生成或更新一份 AGENTS.md 文件，帮助其他 AI 编程助手（以及新加入的开发者）快速理解该代码库。

<env>
  Working directory: ${workingDir}
  Platform: ${platform}
  Today's date: ${today}
</env>

## 项目扫描结果
以下是通过扫描项目目录、模块结构与关键配置文件自动收集到的原始信息：

${scanSummary}

#if($hasExisting)
## 已存在的 AGENTS.md
项目根目录下已存在 AGENTS.md，内容如下。请在此基础上更新、修正过时或不准确的信息，保留仍然有效的内容，而不是简单地全部推倒重写：

${existingContent}
#end

## 任务要求
请直接输出一份完整的 AGENTS.md 文件正文内容（Markdown 格式），至少包含以下部分：
1. **项目概述** —— 这个项目是做什么的，核心功能是什么
2. **技术栈** —— 编程语言、框架、构建工具、主要依赖
3. **目录结构说明** —— 核心目录/模块各自的作用
4. **关键文件说明** —— 入口类、核心业务逻辑所在的文件
5. **开发约定**（如能从扫描结果中推断）—— 代码风格、命名习惯、架构分层等

要求：
- 内容必须真实、基于上面的扫描结果，不要编造扫描结果中不存在的信息
- 简洁清晰，合理使用 Markdown 标题、列表
- 直接输出 AGENTS.md 正文本身，不要用代码块包裹整篇内容，不要添加任何前后缀说明（如"好的，这是为你生成的 AGENTS.md"之类的话）
